import { addDecorator } from '@storybook/react';
import StoryRouter from 'storybook-react-router';

// TODO: Look at the storybook docs and find out what file this should be in.
addDecorator(StoryRouter());
